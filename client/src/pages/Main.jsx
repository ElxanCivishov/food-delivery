import { Header } from "../components";

const Main = () => {
  return (
    <div className="w-screen min-h-screen flex flex-col bg-primary">
      <Header />
    </div>
  );
};

export default Main;
